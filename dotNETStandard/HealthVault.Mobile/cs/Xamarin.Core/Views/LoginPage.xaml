<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:core="clr-namespace:HealthVault.Sample.Xamarin.Core;assembly=HealthVault.Sample.Xamarin.Core"
             x:Class="HealthVault.Sample.Xamarin.Core.Views.LoginPage"
             Title="Login">
    <Frame BackgroundColor="{StaticResource ItemsBackground}">
        <Grid Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Label 
                Grid.Row="0"
                Text="{x:Static core:StringResource.Welcome}" 
                Style="{DynamicResource TitleStyle}"
                HorizontalTextAlignment="Center"
                VerticalOptions="Center" 
                HorizontalOptions="Center" />
            <ActivityIndicator
                Grid.Row="1"
                HeightRequest="20"
                VerticalOptions="Center"
                HorizontalOptions="FillAndExpand" />
        </Grid>
    </Frame>
</ContentPage>